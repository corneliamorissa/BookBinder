{% extends 'base.html.twig' %}

{%  block header %}
    <header class="landing-header">
        <div class="container">
            <nav class="navbar navbar-expand-lg rounded-3" >
                <div class="container-fluid">
                    <a class="navbar-brand" href="{{ path('LogIn') }}">
                        <img src="{{ asset('/public/assets/logo.png') }}" alt="bookbinderlogo" width="200px">
                    </a>

                    <button class="navbar-toggler bg-white"  type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
                        <div class="navbar-nav" id="nav">
                            <a class="nav-link active" aria-current="page" href="{{ path('LogIn') }}">LogIn</a>
                            <a class="nav-link " href="{{ path('SignUp') }}">SignUp</a>
                        </div>
                    </div>
                </div>
            </nav>
        </div>
    </header>
{% endblock %}

{#Content block finished#}
{% block content %}
    <div class="landing-main px-4">
        <div class="container">
            <div class="row justify-content-between">
                <div class="col-lg-5 mb-3 landing-main-left">
                    <h1 id="login_top_trending" class="text-white p-4">Top Trending Books</h1>
                    <div class="landing-main-left-carousel">
                        <div id="carouselExampleCaptions" class="carousel slide" data-bs-ride="false">
                            <div class="carousel-inner">
                                <div class="carousel-item active text-center">
                                    <img id="BookPic" class="rounded-3 w-50 book-image p-4" style="align-items: center; justify-content: center" data-isbn="{{ first_book.isbn }}" src="{{ asset('/public/assets/no_cover.jpg') }}" alt="BookCover">
                                    <div class="d-5 d-md-block ">
                                        <h5 id="booktitle" style="color: #311E69; text-align: center">{{ first_book.title }}</h5>
                                        <h6 id="bookauthor" style="color: #311E69; text-align: center">{{ first_book.author }}</h6>
                                        <p id="bookgenres"  style="color: #311E69; text-align: center">Book followers: {{ first_book.number_of_followers }}</p>
                                    </div>
                                </div>
                                <div class="carousel-item text-center">
                                    <img id="BookPic" class="rounded-3 w-50 book-image p-4" style="align-items: center; justify-content: center" data-isbn="{{ second_book.isbn }}" src="{{ asset('/public/assets/no_cover.jpg') }}" alt="BookCover">
                                    <div class="d-5 d-md-block ">
                                        <h5 id="booktitle" style="color: #311E69; text-align: center">{{ second_book.title }}</h5>
                                        <h6 id="bookauthor" style="color: #311E69; text-align: center">{{ second_book.author }}</h6>
                                        <p id="bookgenres"  style="color: #311E69; text-align: center">Book followers: {{ second_book.number_of_followers }}</p>
                                    </div>
                                </div>
                                <div class="carousel-item text-center">
                                    <img id="BookPic" class="rounded-3 w-50 book-image p-4" style="align-items: center; justify-content: center" data-isbn="{{ third_book.isbn }}" src="{{ asset('/public/assets/no_cover.jpg') }}" alt="BookCover">
                                    <div class="d-5 d-md-block ">
                                        <h5 id="booktitle" style="color: #311E69; text-align: center">{{ third_book.title }}</h5>
                                        <h6 id="bookauthor" style="color: #311E69; text-align: center">{{ third_book.author }}</h6>
                                        <p id="bookgenres"  style="color: #311E69; text-align: center">Book followers: {{ third_book.number_of_followers }}</p>
                                    </div>
                                </div>
                            </div>
                            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
                                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                <span class="visually-hidden">Previous</span>
                            </button>
                            <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
                                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                <span class="visually-hidden">Next</span>
                            </button>
                        </div>
                    </div>
                </div>

                <div class="col-lg-4 landing-main-right landing-welcome">
                    <h1 class="WelcomeLbl"> Welcome </h1>
                    <p class="WelcomeP"> Please enter your username and password</p>
                    {% if error %}
                        <div id="flash">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                    {% endif %}
                        <form action="{{ path('LogIn') }}" method="post">
                            <div class="mb-3">
                                <label style="color: #311E69" for="username" class="form-label">User Name:</label>
                                <input type="text" class="form-control align-self-center w-100"  id="username" name="_username" value="{{ last_username }}" required>
                            </div>
                            <div class="mb-3">
                                <label  style="color: #311E69" for="password" class="form-label">Password:</label>
                                <input class="form-control align-self-center" type="password" id="password" name="_password" required>
                            </div>
                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                             {#If you want to control the URL the user is redirected to on success#}
                            <input type="hidden" name="_target_path" value="/Home">
                            <div class="BtnGroups">
                                <button id='submit_login' class="BookBtns" type="submit">Login</button>
                            </div>
                        </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% set footer_class = 'landing-footer' %}
{% block footer %}
    <footer class="{{ footer_class }}">
        <div>Copyright © 2022 WebTech. KUL All rights Reserved.</div>
        <div><a href="{{ path('privacypolicy') }}" style="color: #311E69 ">  Privacy Policy</a> | <a href="{{ path('termsofservice') }}" style="color: #311E69 "> Terms of Use</a></div>
    </footer>
{% endblock %}
